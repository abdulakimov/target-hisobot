import { Module } from '@nestjs/common';

// M2: Facebook OAuth (ads_read, business_management), long-lived token exchange +
// AES-256-GCM storage, /me/adaccounts sync, Insights client.
@Module({})
export class MetaModule {}
