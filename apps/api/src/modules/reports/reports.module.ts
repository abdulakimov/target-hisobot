import { Module } from '@nestjs/common';

// M4: Report CRUD (account + group + metrics + lead action_type + window + schedule),
// validation, and group pairing (M3) endpoints.
@Module({})
export class ReportsModule {}
