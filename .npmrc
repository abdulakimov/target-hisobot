# Keep dependency resolution strict but allow workspace packages to be linked.
link-workspace-packages=true
prefer-workspace-packages=true
# Hoist nothing surprising; NestJS + Prisma prefer an isolated node_modules.
shamefully-hoist=false
# Run pre/post lifecycle scripts (predev, prebuild) — off by default in pnpm.
enable-pre-post-scripts=true
